{
  "name": "blog.comame.xyz",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "docker run -v $(pwd):/files --rm comameito/puppeteer-nginx",
    "start": "docker run -p 8080:80 -v $(pwd):/files --name blog-comame-xyz-server -d comameito/puppeteer-nginx sh -c \"service nginx start && while :; do sleep 10; done\"",
    "stop": "docker rm -f blog-comame-xyz-server",
    "build-markdown": "node -e \"require('./build').buildMarkdown(require('./archives/entries.json'))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "marked": "^0.8.0",
    "puppeteer": "^2.1.1"
  }
}
